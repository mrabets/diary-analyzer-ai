<%= turbo_stream_action_tag(
      "prepend",
      target: "messages",
      template: %(#{render @messages})
    ) %>
<%= turbo_stream_action_tag(
      "replace",
      target: "pager",
      template: %(#{render "pager", conversation: @conversation, pagy: @pagy})
    ) %>
